# Core ML / numerics
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Exchange data fetching
ccxt>=4.0.0
requests>=2.28.0

# Orderbook reconstruction
# Install from: https://github.com/nickvdyck/ohlcv-to-orderbook
# pip install ohlcv-to-orderbook
ohlcv-to-orderbook

# PUMPABLE COINS extractor (sibling project — install locally)
# Place the PUMPABLE COINS project folder at the same level as this project,
# then install it in editable mode:
#   pip install -e ../PUMPABLE\ COINS
# pumpable-coin-extractor  # uncomment if published to PyPI

# Standard library (no install needed — listed for reference)
# argparse, glob, gzip, io, json, logging, os, platform,
# random, re, shutil, string, subprocess, sys, time,
# typing, urllib, zipfile
